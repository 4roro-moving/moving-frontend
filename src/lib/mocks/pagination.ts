import { buildClientPagination } from "@/lib/utils/pagination";

/**
 * @deprecated `buildClientPagination` 사용
 * // 2026.07.30 정슬기 - [수정] utils로 이전 후 호환 re-export
 */
export const buildMockPagination = buildClientPagination;
